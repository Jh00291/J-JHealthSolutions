<UserControl x:Class="J_JHealthSolutions.Views.VisitControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             HorizontalAlignment="Stretch" VerticalAlignment="Stretch">

    <Grid Margin="0,-47,0,0">
        <!-- Define Grid Rows -->
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- DataGrid for displaying visits -->
        <DataGrid x:Name="VisitsDataGrid"
                  AutoGenerateColumns="False"
                  SelectionMode="Single"
                  SelectionUnit="FullRow"
                  IsReadOnly="True"
                  Margin="0,76,0,10"
                  ColumnWidth="*"
                  SelectionChanged="VisitsDataGrid_SelectionChanged">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Visit ID" Binding="{Binding VisitId}" />
                <DataGridTextColumn Header="Appointment ID" Binding="{Binding AppointmentId}" />
                <DataGridTextColumn Header="Patient ID" Binding="{Binding PatientId}" />
                <DataGridTextColumn Header="Doctor ID" Binding="{Binding DoctorId}" />
                <DataGridTextColumn Header="Visit DateTime" Binding="{Binding VisitDateTime, StringFormat=\{0:MM/dd/yyyy HH:mm\}}" />
                <DataGridTextColumn Header="Weight (kg)" Binding="{Binding Weight}" />
                <DataGridTextColumn Header="Height (cm)" Binding="{Binding Height}" />
                <DataGridTextColumn Header="Blood Pressure (Systolic)" Binding="{Binding BloodPressureSystolic}" />
                <DataGridTextColumn Header="Blood Pressure (Diastolic)" Binding="{Binding BloodPressureDiastolic}" />
                <DataGridTextColumn Header="Temperature (°C)" Binding="{Binding Temperature}" />
                <DataGridTextColumn Header="Pulse" Binding="{Binding Pulse}" />
                <DataGridTextColumn Header="Symptoms" Binding="{Binding Symptoms}" />
                <DataGridTextColumn Header="Initial Diagnosis" Binding="{Binding InitialDiagnosis}" />
                <DataGridTextColumn Header="Final Diagnosis" Binding="{Binding FinalDiagnosis}" />
                <DataGridTextColumn Header="Status" Binding="{Binding VisitStatus}" />
            </DataGrid.Columns>
        </DataGrid>

        <!-- Buttons for Add, Edit, Delete -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="10">
            <Button Content="Add" Width="97" Height="28" Margin="0,0,10,0" Click="AddVisit_Click"/>
            <Button x:Name="EditButton" Content="Edit" Width="97" Height="28" Margin="0,0,10,0" Click="EditVisit_Click" IsEnabled="False"/>
            <Button x:Name="DeleteButton" Content="Delete" Width="97" Height="28" Click="DeleteVisit_Click" IsEnabled="False"/>
        </StackPanel>
    </Grid>
</UserControl>
